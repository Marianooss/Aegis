# Claude Code — AI-Assisted Development Evidence
> Aegis · UiPath AgentHack 2026 · Bonus Points Documentation

## Tool Used
**Claude Code** (Anthropic) — listed as a qualifying coding agent under UiPath for Coding Agents bonus criteria.

---

## How Claude Code Contributed to Aegis

### 1. 4-Layer Validation Engine (`src/core/pipeline.js`)
Claude Code scaffolded the full 4-layer sequential architecture:
- **Layer 1 — Claim Extraction:** Prompt design and claim array structure
- **Layer 2 — Trace Verification:** claim_id join logic between L1 and L2 outputs
- **Layer 3 — Contradiction Detection:** parallel execution pattern with Promise.all
- **Layer 4 — Critical Completeness Scan:** independent analysis + aggregator input

Claude Code generated the initial prompt files in `agents/sentinel/` and iterated on them based on test case outputs across 4 sessions.

### 2. Test Scenario Design (`test-scenarios/`)
Claude Code generated all 7 clinical test cases (TC-001 through TC-007) including:
- Clinically coherent medical notes with embedded failure modes
- Expected verdict + severity + flagged_claims for each scenario
- TC-005 (hyperkalemia K⁺6.8 + 10 critical omissions) — the most clinically sophisticated case, designed to trigger the `escalate_to_human` path

### 3. Dashboard Server (`src/dashboard/server.js`)
Claude Code built the Node.js Express dashboard including:
- Pipeline animation with real-time step tracking
- Diff view (original summary vs corrected summary)
- Audit metrics panel (safe rate, escalation count, correction rate)
- 7 TC runner with pass/fail indicators

### 4. Aggregator Logic (`agents/sentinel/aggregator.js`)
Claude Code designed the join logic that merges Layer 1 claim extraction with Layer 2 trace results using `claim_id` as the primary key — the core deduplication mechanism that enables structured verdict generation.

### 5. Correction Agent Loop (`src/agents/correction.js`)
Claude Code implemented the correction loop:
- Receives SENTINEL verdict with flagged claims
- Generates corrected summary with explicit fixes
- Triggers re-validation against original note
- Does NOT decide escalation — `escalate_to_human` is set independently by the aggregator (`hasCritical || hasHigh`) from the original SENTINEL verdict and is never downgraded by correction success (ADR-009)

---

## Evidence of Integration

The Claude Code output is **not a reference** — it is the working codebase. Every file listed above is directly executable and tested across 7 scenarios.

### Verifiable Evidence
- `docs/claude-code-log.md` — session log documenting 5 Claude Code sessions with specific contributions per session
- `docs/DECISION_LOG.md` — ADR-007 documenting temperature=0 decision with evidence from two TC-007 runs
- `src/core/pipeline.js` — live 4-layer pipeline with L1→L4 sequential execution, temperature=0
- `test-scenarios/TC-001.json` through `TC-007-*.json` — generated and validated scenarios
- `src/dashboard/server.js` — running dashboard at localhost:3000 with export button
- Git commit history — see commits tagged `feat:` and `fix:` for Claude Code-generated features

### Test Evidence
All 7 test cases verified end-to-end with temperature=0. Full exports in `docs/exports/`:
- TC-001: FAIL/HIGH → CORRECTED · ESCALATED TO HUMAN → PASS (inference detection: "within normal limits" not in source)
- TC-002: FAIL/CRITICAL → CORRECTED · ESCALATED TO HUMAN → PASS (hallucinated penicillin allergy, source: NKDA)
- TC-003: FAIL/CRITICAL → CORRECTED · ESCALATED TO HUMAN → PASS (pneumonia confirmed before X-ray result)
- TC-004: FAIL/CRITICAL → CORRECTED · ESCALATED TO HUMAN → PASS (Glibenclamide fabricated, never prescribed)
- TC-005: FAIL/CRITICAL → ESCALATED (K⁺ 6.8 mEq/L omitted, "follow-up in 15 days" vs urgent intervention)
- TC-006: FAIL/CRITICAL → ESCALATED (NKDA contradicted by same-day nursing note ibuprofen allergy)
- TC-007: FAIL/CRITICAL → CORRECTED · ESCALATED TO HUMAN → PASS (pediatric discharge hallucination, 13 flags · generated by Scenario Generator)

**UiPath Agent Builder E2E — TC-007:**
Solution 7 (MedicalRecordsSummarizer) generated a clinically accurate summary.
Solution 6 (SENTINEL Validator) returned PASS/NONE — no false positives on correct summary.
Export: `docs/exports/TC-007-agentbuilder-E2E-PASS-2026-06-21.json`

---

## Coding Agent Role Summary

| Component | Claude Code Role | Integration Status |
|-----------|-----------------|-------------------|
| 4-layer pipeline | Scaffolded + iterated | ✅ Live · temperature=0 |
| Prompt engineering | Designed all 4 layer prompts | ✅ Live in agents/sentinel/ |
| Test scenarios (TC-001–007) | Generated + validated | ✅ Live in test-scenarios/ |
| Dashboard UI | Built from scratch | ✅ Running at localhost:3000 |
| Aggregator join logic | Designed claim_id schema | ✅ Live in aggregator.js |
| Correction loop | Designed corrected-summary generation (proposes fix, does not retry or escalate) | ✅ Live in src/agents/correction.js |
| Scenario Generator | Built CLI + few-shot prompting | ✅ Live in src/core/scenario-generator.js |
| Export audit trail | exportResult() + JSON download | ✅ Live in src/dashboard/server.js |
| DECISION_LOG | ADR-007 temperature=0 decision | ✅ Live in docs/DECISION_LOG.md |

---

*Claude Code was used throughout the development lifecycle — not as a code generator for isolated snippets, but as the primary development partner for architecture decisions, prompt engineering, implementation, debugging, and quality improvements across the full pipeline. Notable: after observing flag count variance across two TC-007 runs (13 vs 14 flags), Claude Code identified the root cause (non-zero temperature) and implemented the fix (`temperature: 0`), documented in `docs/DECISION_LOG.md` ADR-007.*